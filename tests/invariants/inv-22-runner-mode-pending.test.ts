import { describe, it } from 'vitest';

/**
 * INV-22 — owning phase P6.
 * Runner Mode is an offline path. No module reachable from it may reach an agent adapter,
 * and the mode must work with model access hard-disabled.
 */
describe('INV-22 — Runner Mode reaches no agent module and works with model access disabled', () => {
  it.todo(
    'INV-22: pending — owning phase P6 — Runner Mode completes with model access disabled and imports no agent module',
  );
});
