import { describe, it } from 'vitest';

/**
 * INV-25 — owning phase P7.
 * Deletion is real: removing a tool removes its definitions, versions, trials, and events
 * from local storage, not merely hides them.
 */
describe('INV-25 — tool deletion removes definitions, versions, trials, and events from local storage', () => {
  it.todo(
    'INV-25: pending — owning phase P7 — deleting a tool removes its definitions, versions, trials, and events from storage',
  );
});
