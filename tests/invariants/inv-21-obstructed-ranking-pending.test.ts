import { describe, it } from 'vitest';

/**
 * INV-21 — owning phase P5.
 * Scene 5 of the Flight Lab narrative: under version 2, the obstructed trial becomes invalid
 * and the ranking of designs changes.
 */
describe('INV-21 — obstructed trial flips valid→invalid under v2 and the ranking changes', () => {
  it.todo(
    'INV-21: pending — owning phase P5 — replaying trial_004 under tool_version_002 marks it invalid and changes the ranking',
  );
});
