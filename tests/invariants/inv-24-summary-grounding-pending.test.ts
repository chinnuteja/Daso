import { describe, it } from 'vitest';

/**
 * INV-24 — owning phase P7.
 * Every claim in a parent summary maps to an existing event id. A fabricated-but-plausible
 * summary is rejected.
 */
describe('INV-24 — every parent-summary claim maps to an existing event id; fabricated summary rejected', () => {
  it.todo(
    'INV-24: pending — owning phase P7 — a parent summary whose claims do not map to existing event ids is rejected',
  );
});
